# Kubernetes Secret for Vedic Astrology Calculator
# WARNING: Base64 encode all secret values before applying
# Example: echo -n "your-secret-key" | base64
apiVersion: v1
kind: Secret
metadata:
  name: vedic-astrology-secrets
  namespace: vedic-astrology
  labels:
    app: vedic-astrology-calculator
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded secrets
  SECRET_KEY: eW91ci1zdXBlci1zZWNyZXQta2V5LWhlcmUtY2hhbmdlLXRoaXMtaW4tcHJvZHVjdGlvbg==  # your-super-secret-key-here-change-this-in-production
  API_KEY_ENFORCEMENT_ENABLED: dHJ1ZQ==  # true
  
  # Optional: Database credentials if using external database
  # DB_USERNAME: <base64-encoded-username>
  # DB_PASSWORD: <base64-encoded-password>
  
  # Optional: External service credentials
  # SENTRY_DSN: <base64-encoded-sentry-dsn>
  # REDIS_PASSWORD: <base64-encoded-redis-password>